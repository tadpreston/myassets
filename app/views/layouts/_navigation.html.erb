<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse"
              data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%  if current_user %>
        <%= link_to 'Myassets', spaces_path, class: 'navbar-brand' %>
      <% else %>
        <%= link_to 'Myassets', root_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li><%= link_to 'New Space', new_space_path %>
          <li><%= link_to 'Logout', logout_path %></li>
          <li><%= link_to current_user.email, current_user %></li>
        <% else %>
          <li><%= link_to 'Create Account', signup_path %></li>
          <li><%= link_to 'Login', login_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
